throwaway code to get surgery names on same date as sample collection dates from
cbiportal flatfiles

## Run

```
uv run xref.py /path/to/msk_solid_heme > output.csv
```

Optional: `--output output.csv` instead of stdout redirect.
